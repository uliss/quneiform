Main goals are:
    1. code cleanup and such refactoring as removing tons of global variables, cast to void* etc.
    
    2. make cuneiform work on OpenBSD and OpenSolaris platforms (now it compiles, but segfaults)
    
    3. remove a lot of duplicated code
    
    4. add multilanguage support
    
